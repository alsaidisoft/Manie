
@{
    ViewBag.Title = "Tabs";
    string align = "text-right";
    string alignInverse = "text-left";
    string dir = "ltr";
    if (ViewBag.lang == "ar")
    {
        align = "text-left";
        alignInverse = "text-right";
        dir = "rtl";
    }


}

<style>
    td, th {
        text-align: center !important;
    }
</style>



@if (ViewBag.RESPO_STATE > 1 && ViewBag.RESPO_STATE <= 4)
{
    <div class="col-md-12">
        <div class="row">
            <div class="col-md-12">

                <div class="card shadow-sm">
                    <div class="card-body">
                        <div class="row justify-content-center align-self-center">
                            <div class="btn-group">
                                @if (ViewBag.FORCE_TYPE_CODE == 1)
                                {
                                    <a class="btn btn-outline-primary  text-center @if (ViewBag.activetab == "Search")
                                { <text>active</text>} " href="@Url.Action("Search", ViewBag.controllerName, null)">
                                        <i class="fa fa-search"></i>
                                        &nbsp;
                                        @Resources.Passes.ResourceManager.GetString("Security_pass_Search" + "_" + ViewBag.lang)
                                    </a>

                                    <a class="btn btn-outline-primary  text-center @if (ViewBag.activetab == "Otherpermit") { <text>active</text>} " href="@Url.Action("Otherpermit", ViewBag.controllerName, null)">
                                        <i class="fa fa-external-link-square-alt"></i>
                                        &nbsp;
                                        @Resources.Passes.ResourceManager.GetString("Otherpermit" + "_" + ViewBag.lang)
                                    </a>
                                }
                                @*<a class="btn btn-outline-primary  text-center @if (ViewBag.activetab == "Supercreate") { <text>active</text>} " href="@Url.Action("Supercreate", ViewBag.controllerName, null)">
            <i class="fa fa-plus-square"></i>
            &nbsp;
            @Resources.Passes.ResourceManager.GetString("Security_pass_Supercreate" + "_" + ViewBag.lang)
        </a>*@

                                @*<a class="btn btn-outline-primary  text-center @if (ViewBag.activetab == "Otherpermit") { <text>active</text>} " href="@Url.Action("Otherpermit", ViewBag.controllerName, null)">
            <i class="fa fa-external-link-square-alt"></i>
            &nbsp;
            @Resources.Passes.ResourceManager.GetString("Otherpermit" + "_" + ViewBag.lang)
        </a>*@
                                @*@if (ViewBag.FORCE_TYPE_CODE == 3 || ViewBag.DEVELOPER == true)
        {
            <a class="btn btn-outline-primary  text-center @if (ViewBag.activetab == "Companypermit")
        { <text>active</text>} " href="@Url.Action("Companypermit", ViewBag.controllerName, null)">
                <i class="fa fa-expand-arrows-alt"></i>
                &nbsp;
                @Resources.Passes.ResourceManager.GetString("security_company_permit" + "_" + ViewBag.lang)
            </a>
        }*@
                                @if (ViewBag.FORCE_TYPE_CODE == 3)
                                {
                                    <a class="btn btn-outline-primary  text-center @if (ViewBag.activetab == "Otherpermit") { <text>active</text>} " href="@Url.Action("Otherpermit", ViewBag.controllerName, null)">
                                        <i class="fa fa-external-link-square-alt"></i>
                                        &nbsp;
                                        @Resources.Passes.ResourceManager.GetString("Otherpermit" + "_" + ViewBag.lang)
                                    </a>

                                    <a class="btn btn-outline-primary  text-center @if (ViewBag.activetab == "StayAtPortMod")
                                    { <text>active</text>} " href="@Url.Action("StayAtPortMod", ViewBag.controllerName, null)">
                                        <i class="fa fa-anchor"></i>
                                        &nbsp;
                                        @Resources.Passes.ResourceManager.GetString("create_staty_at_port_for_mod" + "_" + ViewBag.lang)
                                    </a>

                                    <a class="btn btn-outline-primary  text-center @if (ViewBag.activetab == "StayAtPortForOthers")
                                    { <text>active</text>} " href="@Url.Action("StayAtPortForOthers", ViewBag.controllerName, null)">
                                        <i class="fa fa-anchor"></i>
                                        &nbsp;
                                        @Resources.Passes.ResourceManager.GetString("create_staty_at_port_for_other" + "_" + ViewBag.lang)
                                    </a>
                                }

                                @*@if (ViewBag.FORCE_TYPE_CODE == 2 || ViewBag.DEVELOPER == true)
        {
            <a class="btn btn-outline-primary  text-center @if (ViewBag.activetab == "ProveIdentity")
            { <text>active</text>} " href="@Url.Action("ProveIdentity", ViewBag.controllerName, null)">
                <i class="fa fa-outdent"></i> <i class="fa fa-expand-arrows-alt"></i>
                &nbsp;
                @Resources.Passes.ResourceManager.GetString("ProveIdentity_private_permit" + "_" + ViewBag.lang)
            </a>
        }*@
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>


}
else
{

    var url = Url.RequestContext.RouteData.Values["id"];
    var id = int.Parse(url.ToString());

    if (ViewBag.RESPO_STATE > 1 && ViewBag.RESPO_STATE <= 4)
    {
    <div class="col-md-12 @align">
        <a href="~/@ViewBag.lang/Mypasses/Select/@id" class="btn btn-outline-dark">
            <i class="fa fa-backward"></i>
            @Resources.Passes.ResourceManager.GetString("back" + "_" + ViewBag.lang)
        </a>
    </div>
    }
}

